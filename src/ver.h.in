#ifndef STEPMANIA_VER_H
#define STEPMANIA_VER_H

#include "version.hpp"

extern char const * const product_version = sm_version::git;

// XXX: These names are misnomers. This is actually the BUILD number, time and date.
extern unsigned long const version_num;
extern char const * const version_time = sm_version::timestamp_time;
extern char const * const version_date = sm_version::timestamp_date;
extern char const * const sm_version_git_hash = sm_version::git_hash;

#endif
